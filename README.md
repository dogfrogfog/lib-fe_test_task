```
let configuration = {
  attributes: ['red', 'blue', 'black'],
  placeholder: '.product-color',
  placeholder_text: 'Surprise me with the random color',
  cart_button: '.cart-btn',
  image: window.location.origin + '/images/blue.png',
  select_attribute: function(attr) {}
}
```